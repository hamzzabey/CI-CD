config:
  target: "https://httpbin.org"
  phases:
    - duration: 1
      arrivalRate: 2
      name: GoIT Performance Test

scenarios:
  - name: Get a resource
    flow:
      - get:
          url: "{{target}}"